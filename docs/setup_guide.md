# 🛠️ Руководство по настройке Modbus RTU Relay и Waveshare Gateway

Это руководство объединяет все шаги для успешного подключения 32-канального реле через Ethernet Gateway.

---

## 1. Конфигурация Waveshare Gateway (VirCOM)

Самый критичный этап. Если настройки неверны, Gateway не будет передавать команды.

### 🔑 "Секретный" алгоритм настройки
Опытным путем мы выяснили, что настройки применяются только в такой последовательности:

1.  **STOP:** Нажмите красную кнопку в VirCOM.
2.  **Настройка:** Внесите изменения (см. таблицу ниже).
3.  **Save As Default:** Обязательно нажмите эту кнопку, чтобы настройки сохранились после перезагрузки.
4.  **Modify Setting:** Нажмите эту кнопку для применения.
5.  **START:** Нажмите зеленую кнопку, чтобы запустить Gateway.

### ⚙️ Таблица настроек

| Раздел | Параметр | Значение | Комментарий |
|--------|----------|----------|-------------|
| **Device Settings** | IP Mode | Static | `192.168.1.254` |
| | Port | 502 | Стандарт Modbus TCP |
| | Work Mode | TCP Server | |
| | **Modbus TCP To RTU** | **✅ Включено** | **КРИТИЧНО!** Без этого не работает. |
| **Advanced Settings** | **Enable Multi-host** | **✅ Включено** | **КРИТИЧНО!** Для работы с ID 1-4. |
| **Serial Settings** | Baud Rate | 9600 | |
| | Data Bits | 8 | |
| | Parity | None | |
| | Stop Bits | 1 | |

---

## 2. Настройка сети на Mac

Чтобы управлять Gateway (`192.168.1.254`) с вашего Mac (который обычно в сети `192.168.0.x`), нужно добавить второй IP-адрес.

### Быстрая команда
Выполните в терминале:

```bash
sudo ifconfig en0 alias 192.168.1.101 255.255.255.0
```
*(Пароль при вводе не отображается)*

### Как проверить
```bash
ping -c 3 192.168.1.254
```
Если пинг идет — связь есть.

> **Важно:** Настройка сбрасывается после перезагрузки Mac.

---

## 3. Использование скриптов

Все скрипты находятся в папке `scripts/`.

### 🧪 Проверка связи (test_connection.py)
Простой тест: подключается к Gateway и щелкает 1-м реле.
```bash
python3 scripts/test_connection.py
```

### 🔄 Тест последовательности (test_sequence.py)
Прогоняет все 32 канала (вкл/выкл).
```bash
python3 scripts/test_sequence.py
```

### 🔍 Сканирование портов (scan_ports.py)
Проверяет доступность устройств на Slave ID 1, 2, 3, 4.
```bash
python3 scripts/scan_ports.py
```

---

## 4. Решение проблем (Troubleshooting)

### Ошибка: `ModbusIOException: No response received`
*   **Причина:** Gateway не конвертирует TCP в RTU.
*   **Решение:** Проверьте галочку **Modbus TCP To RTU** в VirCOM. Убедитесь, что нажали **Save As Default** и перезагрузили Gateway.

### Ошибка: `Connection reset by peer`
*   **Причина:** Gateway сбросил соединение (часто бывает сразу после перезагрузки).
*   **Решение:** Подождите 10-15 секунд и попробуйте снова.

### Ошибка: `Timeout`
*   **Причина:** Нет сетевой связи или конфликт IP.
*   **Решение:** Проверьте пинг. Убедитесь, что у Mac IP `192.168.1.101`, а не `.100` (который занят RPI5).
