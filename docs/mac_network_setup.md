# 🌐 Настройка сети на Mac для доступа к Gateway

Чтобы управлять Gateway (192.168.1.254) напрямую с вашего Mac (который находится в сети 192.168.0.x), нужно добавить **второй IP-адрес** (alias) на ваш сетевой интерфейс.

Это позволит вашему компьютеру быть одновременно в двух сетях:
1.  `192.168.0.x` (Ваша основная сеть, Интернет)
2.  `192.168.1.x` (Сеть Gateway)

---

## 🚀 Команда для добавления IP

Откройте терминал на Mac и выполните:

```bash
sudo ifconfig en0 alias 192.168.1.101 255.255.255.0
```

### Разбор команды:

*   `sudo`: Выполнение от имени администратора (потребует ваш пароль от Mac).
*   `ifconfig`: Утилита настройки сети.
*   `en0`: Имя вашего сетевого интерфейса (Wi-Fi или Ethernet).
    *   *Примечание: Мы проверили, у вас это `en0`.*
*   `alias`: Команда "добавить псевдоним" (дополнительный адрес).
*   `192.168.1.101`: Новый IP-адрес, который мы присваиваем вашему Mac в сети Gateway.
    *   *Важно: Мы используем .101, так как .100 уже занят Raspberry Pi.*
    *   *Важно: Этот адрес не должен конфликтовать с Gateway (192.168.1.254).*
*   `255.255.255.0`: Маска подсети (стандартная для домашних сетей).

---

## ✅ Как проверить, что сработало

1.  **Проверьте настройки интерфейса:**
    ```bash
    ifconfig en0 | grep 192.168.1
    ```
    Должен появиться вывод: `inet 192.168.1.101 netmask 0xffffff00 broadcast 192.168.1.255`

2.  **Проверьте связь с Gateway:**
    ```bash
    ping -c 3 192.168.1.254
    ```
    Должны пойти ответы (64 bytes from...).

---

## 🗑️ Как удалить (если больше не нужно)

Если вы захотите убрать этот дополнительный адрес:

```bash
sudo ifconfig en0 -alias 192.168.1.101
```
*Обратите внимание на минус перед alias (`-alias`).*

---

## ⚠️ Важно знать
Эта настройка **сбрасывается после перезагрузки** компьютера. Если вы перезагрузите Mac, команду нужно будет выполнить снова.
